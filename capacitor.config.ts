import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samsite.bayanihanaid',
  appName: 'BayanihanAid-Mobile',
  webDir: 'www'
};

export default config;
